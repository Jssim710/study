#include <iostream>
using namespace std;


int main(int argc, char *argv[])
{
  auto b1 = begin(arr1), e1 = end(arr1);
  auto b2 = begin(arr2), e2 = end(arr2);
  for (b1 != e1 && b2 != e2) {
  }
  return 0;
}
